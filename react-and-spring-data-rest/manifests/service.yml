apiVersion: v1
kind: Service
metadata:
    name: appcontoso-service
    namespace: microservices-dev
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: appcontoso